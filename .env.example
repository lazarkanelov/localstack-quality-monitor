# LocalStack Quality Monitor - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED - The application will not run without these
# =============================================================================

# Anthropic API key for Claude test generation
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub personal access token with repo scope
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub repository for storing artifacts (format: owner/repo)
# This repo will store discovered architectures, generated tests, and reports
ARTIFACT_REPO=your-org/quality-monitor-artifacts

# =============================================================================
# OPTIONAL - Defaults will be used if not specified
# =============================================================================

# Slack webhook URL for notifications (optional)
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Token budget per run for Claude API (default: 500000)
ANTHROPIC_TOKEN_BUDGET=500000

# LocalStack Docker image version (default: latest)
LOCALSTACK_VERSION=latest

# Number of parallel validations (default: 4)
LSQM_PARALLEL=4

# Timeout per validation in seconds (default: 300)
LSQM_TIMEOUT=300

# GitHub repository for creating issues on failures (default: localstack/localstack)
ISSUE_REPO=localstack/localstack
